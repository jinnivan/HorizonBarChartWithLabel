export declare class MockILocale {
    private currentLocale;
    private locales;
    private static DefaultLocales;
    constructor(locales?: Object);
    set locale(key: string);
    get locale(): string;
}

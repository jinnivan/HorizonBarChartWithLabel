import powerbi from "powerbi-visuals-api";
import ITooltipService = powerbi.extensibility.ITooltipService;
import { ILocalVisualStorageService } from "./mockIStorageService";
import { MockILocale } from "./mockILocale";
import VisualObjectInstancesToPersist = powerbi.VisualObjectInstancesToPersist;
import DrillArgs = powerbi.DrillArgs;
import ISelectionIdBuilder = powerbi.visuals.ISelectionIdBuilder;
import ISelectionManager = powerbi.extensibility.ISelectionManager;
import IColorPalette = powerbi.extensibility.ISandboxExtendedColorPalette;
import IVisualEventService = powerbi.extensibility.IVisualEventService;
import IDownloadService = powerbi.extensibility.IDownloadService;
import HostCapabilities = powerbi.extensibility.HostCapabilities;
import IVisualLicenseManager = powerbi.extensibility.IVisualLicenseManager;
import IWebAccessService = powerbi.extensibility.IWebAccessService;
import IVisualHost = powerbi.extensibility.visual.IVisualHost;
import CustomVisualApplyCustomSortArgs = powerbi.extensibility.visual.CustomVisualApplyCustomSortArgs;
export declare class MockIVisualHost implements IVisualHost {
    private colorPaletteInstance;
    private selectionManager;
    private tooltipServiceInstance;
    private localeInstance;
    private localizationManager;
    private telemetryService;
    private authService;
    private localStorageService;
    private visualEventService;
    hostCapabilities: HostCapabilities;
    downloadService: IDownloadService;
    licenseManager: IVisualLicenseManager;
    webAccessService: IWebAccessService;
    drill: (args: DrillArgs) => void;
    applyCustomSort: (args: CustomVisualApplyCustomSortArgs) => void;
    constructor(colorPalette?: IColorPalette, selectionManager?: ISelectionManager, tooltipServiceInstance?: ITooltipService, localeInstance?: MockILocale, localizationManager?: powerbi.extensibility.ILocalizationManager, telemetryService?: powerbi.extensibility.ITelemetryService, authService?: powerbi.extensibility.IAuthenticationService, storageService?: ILocalVisualStorageService, eventService?: IVisualEventService, hostCapabilities?: HostCapabilities, downloadService?: IDownloadService, licenseManager?: IVisualLicenseManager, webAccessService?: IWebAccessService);
    createSelectionIdBuilder(): ISelectionIdBuilder;
    createSelectionManager(): ISelectionManager;
    get colorPalette(): IColorPalette;
    get locale(): string;
    set locale(language: string);
    applyJsonFilter(filter: powerbi.IFilter, objectName: string, propertyName: string, action: powerbi.FilterAction): void;
    get telemetry(): powerbi.extensibility.ITelemetryService;
    get authenticationService(): any;
    persistProperties(changes: VisualObjectInstancesToPersist): void;
    get tooltipService(): ITooltipService;
    launchUrl(url: string): void;
    get storageService(): ILocalVisualStorageService;
    get eventService(): IVisualEventService;
    get instanceId(): string;
    fetchMoreData(): boolean;
    refreshHostData(): void;
    createLocalizationManager(): powerbi.extensibility.ILocalizationManager;
    switchFocusModeState: (on: boolean) => void;
    hostEnv: powerbi.common.CustomVisualHostEnv;
    displayWarningIcon: (hoverText: string, detailedText: string) => void;
    openModalDialog: (dialogId: string, options?: powerbi.extensibility.visual.DialogOpenOptions, initialState?: object) => powerbi.IPromise<powerbi.extensibility.visual.ModalDialogResult>;
}

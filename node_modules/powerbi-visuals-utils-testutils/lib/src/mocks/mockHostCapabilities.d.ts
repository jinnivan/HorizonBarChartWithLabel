import powerbi from "powerbi-visuals-api";
import HostCapabilities = powerbi.extensibility.HostCapabilities;
export declare class MockHostCapabilities implements HostCapabilities {
    allowInteractions: boolean;
    allowModalDialog: boolean;
    constructor(allowInteractions?: boolean, allowModalDialog?: boolean);
    set hostCapabilities(capabilities: HostCapabilities);
    get hostCapabilities(): HostCapabilities;
}

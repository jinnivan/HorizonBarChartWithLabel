import powerbi from "powerbi-visuals-api";
import IPromise = powerbi.IPromise;
import LicenseNotificationType = powerbi.LicenseNotificationType;
import IVisualLicenseManager = powerbi.extensibility.IVisualLicenseManager;
import visual = powerbi.extensibility.visual;
export declare class MockIVisualLicenseManager implements IVisualLicenseManager {
    getAvailableServicePlans(): IPromise<visual.LicenseInfoResult>;
    notifyLicenseRequired(notificationType: LicenseNotificationType): IPromise<boolean>;
    notifyFeatureBlocked(tooltip: string): IPromise<boolean>;
    clearLicenseNotification(): IPromise<boolean>;
}
